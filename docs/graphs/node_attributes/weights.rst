#########
 Weights
#########

The `weights` is node attribute useful to define the importance of a
node in the loss function. You can set the weights to follow an uniform
distribution or to match the area associated to that node.

.. literalinclude:: ../yaml/attributes_weights.yaml
   :language: yaml
