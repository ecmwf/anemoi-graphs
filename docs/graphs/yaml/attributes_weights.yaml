nodes:
  - name: data
    node_builder:
      _target_: aifs.graphs.nodes.nodes.ZarrDatasetNodeBuilder
      dataset: /path/to/dataset.zarr
    attributes:
      weights:
        _target_: aifs.graphs.nodes.weights.Area
        norm: unit-max
  - name: hidden
