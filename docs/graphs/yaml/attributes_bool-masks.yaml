nodes:
  - name: data
    node_builder:
      _target_: aifs.graphs.nodes.nodes.ZarrDatasetNodeBuilder
      dataset: /path/to/dataset.zarr
    attributes:
      weights:
        _target_: aifs.graphs.nodes.masks.BooleanMask
        variable: lsm
        invert: true
  - name: hidden
